{
  "name": "@spadin/webextension-storage",
  "version": "1.0.4",
  "description": "Helper for storing browser extension settings",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "lint": "tslint --project tsconfig.json",
    "test": "echo \"Error: no test specified\" && exit 1",
    "relink": "npm link @spadin/tsconfig",
    "prepublish": "npm run lint && npm run build"
  },
  "author": "Joel Spadin <joelspadin@gmail.com>",
  "license": "MIT",
  "repository": "https://github.com/ChaosinaCan/webextension-storage",
  "dependencies": {},
  "devDependencies": {
    "@spadin/tsconfig": "^1.3.0",
    "tslint": "^5.16.0",
    "typescript": "^3.4.5",
    "webextension-polyfill-ts": "^0.9.0"
  },
  "peerDependencies": {
    "webextension-polyfill-ts": "^0.9.0"
  }
}
